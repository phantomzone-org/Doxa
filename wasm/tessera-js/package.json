{
  "name": "tessera-js",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:wasm": "cd ../tessera-client-wasm && wasm-pack build --target web --out-dir ../tessera-js/wasm",
    "dev": "vite demo",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^6.0.0",
    "vite-plugin-wasm": "^3.6.0",
    "vitest": "^2.0.0"
  }
}
